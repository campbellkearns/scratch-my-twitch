/**
 * Temporary redirect file for useAuth imports
 * This resolves Vite cache issues when we renamed useAuth.ts to useAuth.tsx
 */

// Re-export everything from the .tsx file
export * from './useAuth.tsx';
